.p-marged
  margin-left: 20px

$displays = inline block inline-block
for $display in $displays
  .{$display}
    display $display

form.aligned
  // align form
  // I've been using that css for what, 15 years now ?
  label
    display: inline-block
    width: 140px
    text-align: right
    margin-right: 20px
